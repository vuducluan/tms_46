<h2><%= t "overview" %></h2>
<h2><%= @subject.name %></h2>
<p><%= @subject.instruction %></p>
<hr />
<h2><%= t "subject.task" %></h2>
<ul class="list-unstyled">
  <% @tasks.each do |task| %>
    <li>
      <h3><%= link_to task.name, "#" %></h3>
      <i><%= t "ago", time: time_ago_in_words(task.created_at) %></i>
    </li>
  <% end %>
</ul>
<%= paginate @tasks %>
