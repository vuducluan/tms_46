'<%= form_for [:admin, @course] do |f| %>'
  $(".page").append("<%= escape_javascript(render 'supervisors', f: f)%>");
'<% end %>'
$("#load_more_link").replaceWith("<%= escape_javascript(link_to_next_page(@users, 'Load More', remote: true, id: 'load_more_link'))%>")
